## Metamask for Bitcoin

This repository provides helpful functions to generate the Bitcoin wallet from MetaMask:
* `generateBitcoinKey` generates the Bitcoin wallet from signed message (a.k.a seed) by MetaMask.
   
